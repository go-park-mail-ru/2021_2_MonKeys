
version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "работает там не"
          command: "echo 'должно'"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
